---
// Awards — edit this array with real competition results.
const awards = [
  {
    place: '1st place',
    title: 'Competition title — edit me',
    sub: 'Organizer / event · 2025',
    desc: 'Short note on what the project did and why it placed — replace with your real competition result.',
  },
  {
    place: 'Finalist',
    title: 'Hackathon / contest — edit me',
    sub: 'Organizer / event · 2024',
    desc: 'Another competition result — edit the placement, title, organizer, year, and description.',
  },
];
---

<section id="awards" class="section">
  <div class="section__head">
    <span class="section__num">06</span>
    <h2 class="section__title" data-cms="awards.title">Awards</h2>
  </div>
  <div class="timeline">
    {
      awards.map((a, i) => (
        <div class="timeline__row">
          <div class="timeline__place">
            <span class="diamond">◆</span>
            <span data-cms={`awards.${i + 1}.place`}>{a.place}</span>
          </div>
          <div>
            <div class="timeline__title" data-cms={`awards.${i + 1}.name`}>{a.title}</div>
            <div class="timeline__sub" data-cms={`awards.${i + 1}.sub`}>{a.sub}</div>
            <p class="timeline__desc" data-cms={`awards.${i + 1}.desc`}>{a.desc}</p>
          </div>
        </div>
      ))
    }
    <div class="timeline__row">
      <div class="timeline__time timeline__time--faint">[ add ]</div>
      <div>
        <div class="timeline__add" data-cms="awards.add">Add another award — edit this entry</div>
      </div>
    </div>
  </div>
</section>
